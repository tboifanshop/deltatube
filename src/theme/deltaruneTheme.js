import { createTheme } from "@mui/material/styles";

export const deltaruneTheme = createTheme({
  typography: {
    fontFamily: `"8bitoperator JVE", ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace`,
  },
});
